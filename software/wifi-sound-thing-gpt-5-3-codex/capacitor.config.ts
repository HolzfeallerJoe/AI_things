import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wifisoundthing.gpt53codex',
  appName: 'wifi-sound-thing-gpt-5-3-codex',
  webDir: 'www'
};

export default config;
